import { monts } from "@/types/fonts"

export default function Footer() {
  return (
    <div className={`${monts.className} text-white h-fit w-full flex flex-col items-center border-t border-[#E89347] mt-4`}>
    asdaasdas
    </div>
  )
}
