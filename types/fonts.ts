import { Inter, Montserrat } from "next/font/google";

export const inter = Inter({
    subsets:['latin'],
    display:'swap'
});


export const monts = Montserrat({
    subsets:['latin'],
    display:'swap'
});